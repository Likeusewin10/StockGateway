# MCP 聚合网关依赖（独立于 REST 取数服务的 .venv-api）
# 安装：python -m venv .venv-mcp && .venv-mcp\Scripts\pip install -r requirements-mcp.txt
fastmcp>=2.0
uvicorn[standard]
httpx
# 让上游 TLS 走操作系统信任库（企业代理/杀软 TLS 检测根证书只在 OS 库、不在 certifi 时必需）。
truststore
pytest
