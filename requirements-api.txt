# HTTP/WebSocket 服务依赖（在 .venv-api 下安装：pip install -r requirements-api.txt）。
# 两个股票 SDK（EmQuantAPI / iFinDAPI）另见 requirements-em.txt / requirements-ths.txt。
fastapi==0.137.2
uvicorn==0.49.0
starlette==1.3.1
pydantic==2.13.4
pandas==3.0.3
# WebSocket 实时推送：uvicorn 的 WS 协议实现，缺它 /em/ws、/ths/ws 无法握手。
websockets==16.0
