# HTTP/WebSocket 服务依赖（在 .venv-api 下安装：pip install -r requirements-api.txt）。
# 两个股票 SDK（EmQuantAPI / iFinDAPI）另见 requirements-em.txt / requirements-ths.txt。
fastapi==0.137.2
uvicorn==0.49.0
starlette==1.3.1
pydantic==2.13.4
pandas==3.0.3
# WebSocket 实时推送：uvicorn 的 WS 协议实现，缺它 /em/ws、/ths/ws 无法握手。
websockets==16.0

# 测试（dev）：starlette 1.3.1 的 TestClient 依赖 httpx2。
pytest==8.4.2
pytest-cov==7.0.0
httpx2==2.0.0
# 压测（dev）：loadtest/crash_recovery.py 按端口定位 PID、ws_soak.py 采样 RSS。
# 缺失时 crash_recovery 回退 netstat/taskkill；ws_soak 的 RSS 采样则跳过。
psutil==6.1.1
